# TMDB v3 API key (free). Get one at https://www.themoviedb.org/settings/api
# after creating an account and requesting a "Developer" API key.
TMDB_API_KEY=

# No key needed for JustWatch: the app talks to their unofficial public GraphQL
# endpoint directly (see README for details and caveats).
